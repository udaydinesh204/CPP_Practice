#include <bits/stdc++.h>
using namespace std;


int main(){
 deque<T> ;<int> dq;
 dq.push_back(3);
 dq.emplace_back(6);//{3,6}
 
 dq.push_front(0);//{0,3,6}
 dq.emplace_front(-3);
 
 dq.pop_back();
 dq.back();
 
 dq.pop_front();
 dq.front();
 
 //rest functions same as vector ;
}